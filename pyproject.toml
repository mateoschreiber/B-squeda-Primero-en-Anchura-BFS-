[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bfs-junta-saneamiento"
version = "1.0.0"
description = "Simulacion academica de conectividad con BFS"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
